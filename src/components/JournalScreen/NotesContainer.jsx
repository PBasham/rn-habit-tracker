import { StyleSheet, Text, View } from 'react-native'

const NotesContainer = () => {
  return (
    <View style={styles.notesContainer}>
      <Text>NotesContainer</Text>
    </View>
  )
}

export default NotesContainer

const styles = StyleSheet.create({
    notesContainer: {

    },
})